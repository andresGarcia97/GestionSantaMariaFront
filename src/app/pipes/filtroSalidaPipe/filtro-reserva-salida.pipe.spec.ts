import { FiltroSalidaPipe } from './filtro-reserva-salida.pipe';

describe('FiltroReservaSalidaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSalidaPipe();
    expect(pipe).toBeTruthy();
  });
});
