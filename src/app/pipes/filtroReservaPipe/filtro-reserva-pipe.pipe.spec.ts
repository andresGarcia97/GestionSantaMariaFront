import { FiltroReservaPipe } from './filtro-reserva-pipe.pipe';

describe('FiltroReservaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroReservaPipe();
    expect(pipe).toBeTruthy();
  });
});
